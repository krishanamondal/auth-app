#spring:
#  application:
#    name: auth-app
server:
  port: 8080
#  configuration
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/auth-app
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect